import React from 'react';

const fallback = () => (
  <div>
    <h1>This is fallback page when device is offline </h1>
    <p>Route will fallback to this page</p>
  </div>
);

export default fallback;